In UNIX everything is a file.
And a way to communicate with file is to use FILE DESCRIPTORS.

A file descriptor is simply an integer associated with an open file.
That file can be a text file, a .c file containing code.
But that file can also be network communication, pipe, terminal and everything else.
