The part of the http request.
It is used to initialize an on the server.

Firstly it contains the Method :
GET, POST, PUT, DELETE...

The Request Target :
Its basically the path or the endpoint
/api/register

The HTTP version :
Usually HTTP/1 or HTTP/2

So the full HTTP request would look like this :
``GET /api/users HTTP/1.1
 