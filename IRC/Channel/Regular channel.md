Use **'#'** as name prefix
As its name suggest, is what's referred to as a normal channel.
The first client that joins the channel is made a channel operator.
On most server, newly created regular channels have then
- protected topic " +t flag".
- no external messages "+n flag".
